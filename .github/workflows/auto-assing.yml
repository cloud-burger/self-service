name: 'Auto Assign'
on: 
  pull_request:
    types: [opened, ready_for_review]

jobs:
  auto-assign:
    runs-on: ubuntu-latest
    steps:
      - uses: kentaro-m/auto-assign-action@1.2.1
        with:
          configuration-path: '.github/auto-assing.yml'
      